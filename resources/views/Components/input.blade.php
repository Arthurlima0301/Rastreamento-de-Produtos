<div class="flex flex-col gap-2">
    <label for="{{ $name }}">{{ $label }}</label>
    <input type="text" name="{{ $name }}" id="{{ $name }}" class="min-w-[300px] border border-stroke rounded-md p-2" required>
</div>
